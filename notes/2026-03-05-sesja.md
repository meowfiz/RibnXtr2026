## Sesja 2026-03-05

Ten plik uzupełnia brakujący dziennik dnia, w którym robione były tylko drobne porządki konfiguracyjne, ale chcemy zachować pełną ciągłość sesji.

### Plan na dziś
- [x] Uporządkować szablony `config.ini` tak, aby łatwo było pracować na dwóch maszynach.

### Co zrobione
- [x] [11:30] Przegląd istniejących konfiguracji `config.ini` w `run_files/`.
- [x] [12:00] Dodano szablony `config.machine1.ini` / `config.machine2.ini` i ustalono, że lokalny `config.ini` jest kopią jednego z nich, nie wersjonowaną w git (commit `Szablony config.ini per maszyna`).

### Notatki / Decyzje
- `config.ini` jest **lokalny dla maszyny** i ignorowany przez git.
- W repo trzymamy tylko szablony `config.sample.ini`, `config.machine1.ini`, `config.machine2.ini` jako punkt odniesienia dla ścieżek i ustawień.
- Przy zmianach konfiguracji na nowej maszynie aktualizujemy odpowiedni szablon, a następnie lokalnie kopiujemy go do `config.ini`.

### Otwarte pytania
- [ ] Czy potrzebny jest dodatkowy szablon dla kolejnych maszyn / środowisk (np. laptop 2, serwer)?

